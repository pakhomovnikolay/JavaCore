public class Manager extends Employee {

    public Manager(String surName, String name, double salary, Integer year, Integer month, Integer day) {
        super(surName, name, salary, year, month, day);
    }
}
